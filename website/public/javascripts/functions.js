function download(name, path) {
	console.log(path);
	/*
	request({
		uri: path,
	}, function(error, response, body) {
		console.log(body);
	});
		
	/*
	var file = fs.createWriteStream(name + '.html');
	var request = http.get(path, function(response) {
		response.pipe(file);
	});*/
}